import React from "react";
import FormExchange from "../form_exchange/form_exchange";

/**
 * Container component for the exchange calculator form.
 * Renders the FormExchange component.
 */
const FormExchangeContainer = () => {
  return <FormExchange />;
};

export default FormExchangeContainer;
